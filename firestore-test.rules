rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 테스트용: 인증된 사용자에게 모든 읽기/쓰기 권한 허용
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}