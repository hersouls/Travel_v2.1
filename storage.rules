rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // 모든 파일: 익명 로그인 포함 인증된 사용자에게 전체 권한 허용
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}