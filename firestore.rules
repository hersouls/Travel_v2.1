rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 모든 컬렉션: 익명 로그인 포함 인증된 사용자에게 전체 권한 허용
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}